server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Let React Router handle routing
    location / {
        try_files $uri /index.html;
    }

    # API calls go to backend container
    location /api/ {
        proxy_pass http://backend:8080;
    }
}
